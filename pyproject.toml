[project]
name = "a-sabr-python"
version = "0.1.0"
description = "Python bindings for A-SABR"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
